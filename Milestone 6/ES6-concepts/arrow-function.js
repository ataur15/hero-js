const add = (num1, num2, num3) => num1 + num2 + num3;
const output1 = add(50, 60, 40);
console.log(output1);

const tenTimes = (num1) => num1 * 10;
const output2 = tenTimes(10);
console.log(output2);

const doMath = (x, y) => {
    const addition = x + y;
    const multiply = addition * 5;
    const divistion = multiply / 6;
    return divistion;
}

const output3 = doMath(20, 10);
console.log(output3);